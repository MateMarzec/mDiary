import React from 'react';
import logo from '../images/logo.png';

 const Logo = () => (
    <div className="logo">
        <img className="logo__image" src={logo} alt='Logo'/>
    </div>
);

export default Logo;